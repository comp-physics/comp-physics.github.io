{
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "pagefind": "^1.5.0"
  },
  "scripts": {
    "build": "bundle exec jekyll build && npx pagefind@1.5.0 --site _site"
  }
}
