{
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  },
  "scripts": {
    "build": "bundle exec jekyll build && npx pagefind --site _site"
  }
}
